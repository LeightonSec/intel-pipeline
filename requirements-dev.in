# Dev/test/lint tooling, compiled to hash-locked requirements-dev.txt with:
#   uv pip compile requirements-dev.in --universal --python-version 3.10 --generate-hashes -o requirements-dev.txt
# Includes the runtime lock so CI installs everything from one hash-locked file.
-r requirements.txt
pytest
pytest-cov
ruff
